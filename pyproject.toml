[project]
name = "app"
version = "0.1.0"
description = ""
authors = [
    {name = "Shota Suzuki",email = "ssuzuki@harvest-w.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
